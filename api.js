// ! API: Application Programming Interface
// request response cycle
// acts as communication bridge between client and server
// client side= frontend= ui(user interface) =customer side
// server side= business logic =backend
// starts from request and ends with response

// ? request has methods
// POST : add/create
// GET : read/ retrieve
// PUT : edit? update
// DELETE : delete/remove

// ? response has status code
// it can either be SUCCESS or ERROR
// success => 200-299
// error can be client side or server side
// client side => 400 to 499
// server side => 500 to 599

// ? Portal
// Web App
// ? UI=> Vanilla, React, Vue, Angular, Svelte
// ?

// ? Mobile

// 1. Hybrid
// ? Flutter
// ? React Native

// 2. Native
// ? Android(kotlin/java)
// ? Swift(ios)

// ? Server is same
// ? Database => permanently store data
// ? Server side language
// ? Java => Spring Boot
// ? Js => Express/ Next js
// ? python = Django/ Flask
// ? php => Laravel

// API
// ? REST => Representational Stateless
// ? SOAP
// ? GraphQlL => client has power to decide which data to fetch
